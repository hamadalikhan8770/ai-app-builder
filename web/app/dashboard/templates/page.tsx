import EmptyState from '@/components/dashboard/EmptyState';
import TemplateCard from '@/components/dashboard/TemplateCard';
import UseTemplateButton from '@/components/dashboard/UseTemplateButton';
import Badge from '@/components/ui/Badge';
import { getTemplates } from '@/lib/dashboard/queries';
export default async function TemplatesPage({ searchParams }: { searchParams: Promise<{ template?: string }> }){const [{template},templates]=await Promise.all([searchParams,getTemplates()]); const selected=templates.find(t=>t.id===template); return <div className="space-y-6"><div><h1 className="text-3xl font-black">Templates</h1><p className="text-slate-400">Browse marketplace templates and start a project from a template.</p></div>{selected&&<div className="card"><div className="flex items-start justify-between gap-4"><div><h2 className="text-xl font-black">Use {selected.title}</h2><p className="mt-2 text-slate-400">This calls the secure use-marketplace-template Edge Function.</p></div><Badge tone={selected.access_type==='premium'?'amber':'green'}>{selected.access_type}</Badge></div><div className="mt-4"><UseTemplateButton templateId={selected.id}/></div></div>}{templates.length? <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{templates.map(t=><TemplateCard key={t.id} template={t}/>)}</div>:<EmptyState title="No templates" message="Published marketplace templates will appear here."/>}</div>}
