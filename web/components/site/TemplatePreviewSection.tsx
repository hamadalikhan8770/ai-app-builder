import { templates } from '@/lib/constants';
export default function TemplatePreviewSection() { return <section className="section"><div className="flex items-end justify-between gap-6"><div><h2 className="text-3xl font-black">Template marketplace preview.</h2><p className="mt-3 text-slate-400">Start faster with app blueprints for popular industries.</p></div><a href="/templates" className="btn-secondary">View all</a></div><div className="mt-10 grid gap-5 md:grid-cols-2 lg:grid-cols-4">{templates.slice(0,8).map((t)=><div key={t.title} className="card"><p className="text-sm text-blue-200">{t.category}</p><h3 className="mt-2 font-black">{t.title}</h3><p className="mt-2 text-sm text-slate-400">{t.stack}</p><span className="mt-4 inline-flex rounded-full bg-white/10 px-3 py-1 text-xs font-bold">{t.access}</span></div>)}</div></section>; }
