import LogoutButton from './LogoutButton';
import type { Profile } from '@/lib/dashboard/types';
export default function DashboardTopbar({ profile, configured }: { profile: Profile | null; configured: boolean }) { return <div className="sticky top-0 z-40 flex items-center justify-between border-b border-white/10 bg-slate-950/80 px-5 py-4 backdrop-blur"><div><p className="text-sm text-slate-400">Web Dashboard</p><h1 className="font-black">{profile?.full_name || profile?.email || (configured ? 'Builder' : 'Local preview')}</h1></div><div className="flex items-center gap-3"><span className="hidden rounded-full bg-white/10 px-3 py-1 text-xs font-bold sm:inline-flex">{profile?.subscription_tier || (configured ? 'free' : 'offline')}</span><LogoutButton configured={configured} /></div></div>; }
